# SKILL: backend engineering team Knowledge Base

> Auto-generated by `devask skill generate` on 2026-05-21 01:45:46
> Team ID: `22057c0b-35ce-468d-abba-cb6d4a94932b`

## About This Team

| Field | Value |
|---|---|
| **Name** | backend engineering team |
| **Description** | backend |
| **Tech Stack** | go, react, sql |

## Knowledge Base Coverage

This knowledge base contains 1 ingested document(s):

- `devask-context.md`

## How to Query

### CLI
```bash
devask ask "How does backend handle deployments?"
devask docs
```

### REST API
```bash
curl -X POST http://localhost:8081/ask \
  -H "Content-Type: application/json" \
  -d '{"team_id":"22057c0b-35ce-468d-abba-cb6d4a94932b","question":"How does backend handle deployments?"}'
```

### MCP Tools (via Claude Desktop)
After running `devask mcp generate`, Claude can use:
- `ask_backend_engineering_team(question)` — Ask any question to the knowledge base
- `list_backend_engineering_team_docs()` — List all ingested documents

## Notes
- Answers are synthesized by **gpt-oss:120b** (via Ollama) using hybrid retrieval (Pinecone + BM25 + RRF).
- Upload new documents with `devask upload <file>` to keep the knowledge base current.
